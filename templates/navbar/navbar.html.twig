

<header>


    <nav class="nav" >
        <div class="container-fluid"  >
            <div class="logo">
                <a href="{{ path('home') }}"><img src="{{ asset('img/navbar.png') }}" alt="Jodelap Coaching" style="width: 250px"></a>
            </div>
            <div id="mainListDiv" class="main_list" >

                <ul class="navlinks" >
                    <li><a class="nav-link active" href="{{ path('home') }}"><i class='bx bx-home-alt' ></i>Accueil</a></li>
                    <li><a class="nav-link active" href="{{ path('evenement') }}">Cours collectifs</a></li>
                    <li><a class="nav-link active" href="{{ path('coaching') }}">Cours particuliers</a></li>
                    <li><a class="nav-link active" href="{{ path('coffret') }}">Coffrets cadeaux</a></li>
                    <li><a class="nav-link active" href="{{ path('avis') }}">Mes avis</a></li>
                    <li><a class="nav-link active" href="{{ path('galerie') }}">Galerie</a></li>
                    {% if  app.session.get('coaching')  or app.session.get('coffret')  or  app.session.get('evenement')   %}
                        <li><a class="nav-link active" href="{{ path('panier') }}"><i class='bx bx-cart-alt'></i>Commande</a></li>
                    {% endif %}
                    {% if app.user %}
                        <li><a class="nav-link active" href="{{ path('user') }}"><i class='bx bx-male-female' ></i>Votre profil</a></li>
                        <li><a class="nav-link active" href="{{ path('app_logout') }}"><i class='bx bx-log-out' ></i>Déconnexion</a></li>
                    {% else%}
                        <li><a class="nav-link active" href="{{ path('app_login') }}"><i class='bx bx-log-in'></i>Connexion/Inscription</a></li>
                    {% endif %}
                </ul>

            </div>
            <span class="navTrigger">
                <i></i>
                <i></i>
                <i></i>
            </span>
        </div>
    </nav>


</header>

<script>
    $('.navTrigger').click(function () {
        $(this).toggleClass('active');
        $("#mainListDiv").toggleClass("show_list");
        $("#mainListDiv").fadeIn();

    });

</script>
<script>
    $(window).scroll(function() {
        if ($(document).scrollTop() > 50) {
            $('.nav').addClass('affix');
        } else {
            $('.nav').removeClass('affix');
        }
    });
</script>