<header>

    <nav id="navbar" class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"><i class='bx bx-cycling'>menu</i></span>
            </button>
            <div class="collapse  navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav    me-auto  mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('evenement') }}">Évènements</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('coaching') }}">Coaching</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('coffret') }}">Coffrets cadeaux</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('galerie') }}">Galerie</a>
                    </li>
                    <li class="nav-item active dropdown">
                        <a class="nav-link active dropdown-toggle" href="{{ path('avis') }}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Avis</a>
                        <ul class="dropdown-menu active" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('avis') }}">Voir mes avis</a></li>
                            <li><a class="dropdown-item" href="{{ path('avis_ajouter') }}">Laisser un avis</a></li>
                        </ul>
                    </li>
                    {% if  app.session.get('coaching')  or app.session.get('coffret')  or  app.session.get('evenement')   %}
                    <li class="nav-item ml-5">
                        <a class="nav-link active" href="{{ path('panier') }}">Panier</a>
                    </li>
                    {% endif %}
                </ul>
                <div class="d-flex active">

                    <ul class="navbar-nav me-auto active mr-5 mb-2 mb-lg-0">
                            {% if app.user %}
                        <li class="nav-item active dropdown">
                            <a class="nav-link active dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Salut {{ app.user.FirstName}} ! Ton compte :
                            </a>
                            <ul class="dropdown-menu active" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>

                            </ul>
                        </li>
                    </ul>
                    {% else%}
                    <ul class="navbar-nav me-auto active mr-5 mb-2 mb-lg-0">

                                <li class="nav-item active dropdown">
                                    <a class="nav-link active dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        Identifiez-vous
                                    </a>
                                    <ul class="dropdown-menu active" aria-labelledby="navbarDropdown">
                                        <li><a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a></li>
                                        <li><a class="dropdown-item" href="{{ path('app_register') }}">S'inscrire</a></li>
                                    </ul>
                                </li>
                            </ul>
{% endif %}
                </div>
            </div>
        </div>
    </nav>


</header>

